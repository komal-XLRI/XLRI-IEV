/**
 * Stub for the `server-only` package.
 *
 * That package throws unless it is resolved under the `react-server`
 * condition, which Vitest does not set. Aliasing it here lets integration
 * tests import the real service modules unchanged.
 */
export {};
